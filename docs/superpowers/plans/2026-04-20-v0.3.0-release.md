# v0.3.0 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Perform a proper release by synchronizing Cargo.toml with a new git tag v0.3.0 and initializing a changelog.

**Architecture:** Standard Rust workspace release flow (bump manifest -> sync lock -> commit -> tag -> build -> push).

**Tech Stack:** Rust, Cargo, Git, Makefile.

---

### Task 1: Quality Gates

**Files:**
- N/A (Build/Test)

- [ ] **Step 1: Run linting**
Run: `make lint`
Expected: PASS (No linting errors)

- [ ] **Step 2: Run tests**
Run: `make test`
Expected: PASS (All 230+ tests pass)

### Task 2: Initialize CHANGELOG.md

**Files:**
- Create: `CHANGELOG.md`

- [ ] **Step 1: Create CHANGELOG.md with v0.3.0 entries**
Write:
```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.3.0] - 2026-04-20

### Added
- Implement agent-driven scheduled tasks.
- Improve Slack integration: Socket Mode, Block Kit, Thread Context.
- Support for opencode provider text/reasoning events.
- OPENCODE_SKIP_PERMISSIONS config and --dangerously-skip-permissions flag.

### Fixed
- Slack webhook output routing after ack.
- Context filtering after boundary turns.
- Telegram retry-after integer/string parsing.
```

- [ ] **Step 2: Commit changelog initialization**
Run: `git add CHANGELOG.md && git commit -m "docs: initialize CHANGELOG.md with v0.3.0 entries"`

### Task 3: Version Bump

**Files:**
- Modify: `Cargo.toml`

- [ ] **Step 1: Update root Cargo.toml version**
Modify: `[workspace.package].version = "0.2.0"` -> `version = "0.3.0"`

- [ ] **Step 2: Sync Cargo.lock**
Run: `cargo check`
Expected: PASS (Cargo.lock updated)

- [ ] **Step 3: Commit version bump**
Run: `git add Cargo.toml Cargo.lock && git commit -m "chore: bump version to 0.3.0"`

### Task 4: Tagging

**Files:**
- N/A (Git)

- [ ] **Step 1: Create annotated tag**
Run: `git tag -a v0.3.0 -m "Release v0.3.0"`

### Task 5: Verification

**Files:**
- N/A (Build)

- [ ] **Step 1: Build release binary**
Run: `make release`
Expected: SUCCESS (Binary created at `target/release/coconutclaw`)

- [ ] **Step 2: Verify version in binary**
Run: `./target/release/coconutclaw --version`
Expected: `coconutclaw 0.3.0` (Assuming --version is implemented, otherwise check logs/output)

### Task 6: Push

**Files:**
- N/A (Git Remote)

- [ ] **Step 1: Push to origin (assuming main branch)**
Run: `git push origin main && git push origin v0.3.0`
Expected: SUCCESS
